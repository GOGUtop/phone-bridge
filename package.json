{
  "name": "anima-phone-bridge",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "SillyTavern virtual phone with an Anima memory bridge",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check core.mjs && node --check world.mjs && node --check world-ui.mjs && node --check memory.mjs && node --check server-plugin/anima-phone-bridge-server/index.mjs"
  }
}
